# PRODUCTION (DIC-1871): there is no local Map Buddy. The viewer calls the Cloud Run
# service directly (COUNTY.endpoints.mapBuddy), so this path is always a 404 here.
location /map-buddy-api/ {
    return 404;
}
